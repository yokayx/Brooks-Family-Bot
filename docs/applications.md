# Заявки в семью

Порт логики KillaBot → BrooksBot без ЧС. Ког `bot/cogs/applications.py`.

## ID

| Назначение | ID |
|------------|-----|
| Канал заявок (меню со списком) | `1453123004308390116` |
| Канал панели управления | `1553239900176908289` |
| Роль состава Main (тег в меню) | `1550827936641454141` (Test) |
| Роль состава «онли VZP» (тег в меню) | `1553233826451431494` |
| Категория открытых тикетов | `1552394606975516804` |
| Категория закрытых тикетов | `1552394634616111178` |
| Recruiter | `1453123003645952254` |
| Test (выдаётся при принятии) | `1550827936641454141` |
| Семейная роль (выдаётся при принятии) | `1552378409211142174` |

## Кто что может

| Действие | Кто |
|----------|-----|
| `/настроить-форму вид: Main\|VZP`, `/отправить-меню-заявок`, `/панель`, `/отправить-кнопку-заявки` | Руководство (`is_leader`) |
| Кнопка «Подать заявку» | Не семья и не руководство; нет открытого тикета; прошло 24 ч с прошлой подачи |
| «Взять заявку» | Руководство или Recruiter |
| Освободить / принять / отклонить | Тот, кто взял, или руководство |

## Как подаётся заявка

Модалки нет: выбрал состав в списке — **сразу открывается тикет** в категории
открытых заявок. Бот кладёт туда один эмбед — «Новая заявка · Main/VZP» с
заявителем, номером тикета и **формой заявки** (список вопросов) — и всё.

Дальше человек заполняет форму сам: пишет ответы в канал тикета вручную. Бот не
задаёт вопросы и не ждёт ответов.

Единственная автоматика: как только заявитель написал **первое** сообщение в
тикете, бот тегает рекрутов `1453123003645952254` и `1453123003645952259`
(один раз на заявку, флаг `tickets.applicant_replied`). Заявку забирает
рекрутер или руководство кнопкой «Взять заявку».

## Панель управления

Панель публикуется **сама при старте бота** в канале `1553239900176908289`
(руководство), id сообщения в `bot_messages` (`control_panel`); удалили —
отправит заново. Отдельной команды для публикации нет.

Если Discord не показывает новые слэш-команды — `/синк` (руководство), он
принудительно пересинхронизирует дерево команд на сервере.

Эмбед панели показывает статусы кастомными эмодзи `on`/`off`
(`APPLICATION_OPEN_EMOJI_ID` / `APPLICATION_CLOSED_EMOJI_ID`):

```
:on: Набор Young
:off: Набор Test
```

| Кнопка | Что делает |
|--------|-----------|
| Набор Young | переключатель: включить, если выключен, выключить, если включён |
| Набор Test | то же самое для состава VZP |
| Форма Young / Форма Test | менеджер вопросов выбранного вида |

Цвет кнопки-переключателя зависит от состояния: **зелёная** (success), когда
набор открыт, **красная** (danger), когда закрыт. Кнопки форм всегда серые.

После переключения панель и меню заявок перерисовываются сразу, поэтому цвета и
список составов всегда актуальны.

После смены статуса бот перерисовывает сообщение с меню заявок (`:on:`/`:off:`).
Закрытый набор: в списке заявок вместо формы — «Набор на этот состав сейчас
приостановлен».

ЧС нет: отклонение только с причиной, без роли и записи в чёрный список.

## Как работает

0. Меню заявок публикуется **само при старте бота**, без команд: описание двух
   составов, их требования, **статус набора** (`:on:` / `:off:`) и
   раскрывающийся список — всё одним **эмбедом**:

   - `Заявка на Young` — «Заполнить заявку в семью» → форма **Main**;
   - `Заявка на Test` — «Заполнить заявку в семью на VZP» → форма **VZP**.

   Статусы набора — кастомным эмодзи и словами: `<:on:…> Набор открыт` /
   `<:off:…> Набор закрыт` (эмодзи: `<:on:1553261250916515871>`,
   `<:off:1553261237461196845>`, они же в панели управления).

   В раскрывающемся списке — **только открытые составы**: закрытый набор не
   показывает вариант «Заявка на …». Если закрыты оба — списка нет вовсе,
   остаётся один эмбед со статусами.

   id сообщения хранится в `bot_messages` (`applications_menu`). При старте бот
   его перерисовывает; если сообщение удалили — отправляет новое в канал заявок.
   Текст обновляется с панели управления при смене статуса набора.
0. Формы — свои на каждый вид: `/настроить-форму вид: Main` и `вид: VZP`,
   до 5 активных вопросов на вид. Вопросы лежат в `application_questions.kind`.
   Вид заявки пишется в `tickets.kind` и виден в заголовке тикета.
1. Руководство: `/настроить-форму` — до 5 активных вопросов. `/отправить-кнопку-заявки` постит кнопку в канал заявок (живёт после рестарта).
2. Человек жмёт кнопку → модалка с вопросами → канал `заявка-{ник}` в категории открытых тикетов. Ответы в эмбеде + SQLite (`tickets.answers_json`).
3. Стафф берёт заявку. Кнопки: освободить, принять, отклонить.
4. **Принять:** роли Test + семейная; ЛС и сообщение в тикете (ник по форме `[Nick] | Имя`, день рождения); канал закрывается через 1 час (перенос в закрытые, у заявителя снимается доступ).
5. **Отклонить:** модалка с причиной → ЛС + сообщение в канале → сразу в закрытые.
6. После принятия: кнопки «Установить никнейм» и «Указать день рождения» (`17.05` / `17 мая` / `may 17`, таймзона не нужна). ДР пишется в `birthdays`.
